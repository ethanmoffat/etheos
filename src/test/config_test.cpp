#include <gtest/gtest.h>

#include "../config.hpp"

GTEST_TEST(ConfigTests, TestTest)
{
    Config config;
    ASSERT_TRUE(true);
}

GTEST_TEST(ConfigTests, TestTest2)
{
    Config config;
    ASSERT_TRUE(false);
}